import React from 'react';
import NavLink from './navbarElements';

export default function NotLoggedUserLeft() {
    return (
        <>
            <NavLink to="/apply">Apply</NavLink>
        </>
    );
}
